# 市场安全策略

## 信任边界

本项目没有账号服务器。用户名不是全局身份凭据，包归属只认 ed25519 公钥指纹。
本地用户名可以重复；正式发布时市场为指纹分配唯一且稳定的 `market_handle`。

一个包有两份针对**同一份规范 manifest 字节**的独立签名：

- 创作者签名 `package.manifest.creator.sig`：证明这些字节来自该创作者密钥；
- 维护者签名 `package.manifest.maintainer.sig`：证明市场审核并接纳了这些相同字节。

创作者签名适用于本地和 P2P 分享，但不代表市场背书。市场来源必须同时通过维护者整包签名。
客户端还必须先验证 `catalog.json.sig`，再解析目录，并拒绝 sequence 或生成时间回退的旧目录。
签名、哈希、文件集合、作者归属或回放检查任一失败，均 fail-closed。

## 投稿态与发布态隔离

- 外部贡献者只写 `submissions/`；正式目录由维护者生成。
- PR CI 只静态读取投稿：规范 JSON、ed25519、哈希、路径、AST、safe YAML、凭据模式、DCO。
- PR CI 不 import、不 exec、不启动投稿插件，也不获得维护者私钥。
- 动态行为测试只能由维护者在一次性、无凭据、受限网络的隔离环境中人工进行。
- 正式发布要求维护者整包签名和有效的目录签名；只有创作者签名的内容不能进入发布态。

## 私钥红线

- 创作者私钥保存在用户本地身份库，不进入分享包、投稿、日志或错误信息。
- 维护者私钥只存在于受控冷存储，不进入仓库、CI、便携包或在线主机。
- `*.key`、私钥 PEM、`.env`、Token、Cookie 和真实凭据不得投稿。
- 模板只能用 `secret://name` 表示运行时凭据引用。
- 公钥和指纹不是秘密，可以随包和市场分发。

## 权限与执行

- 插件默认使用 subprocess 隔离；`in_process` 是需要额外人工论证的高风险例外。
- 权限、域名、输入文件和依赖必须最小化，并在 `listing.md` 解释。
- 私下分享包安装前显示来源、指纹、权限和域名；用户明确确认后只授予“信任此包”，
  不会默认信任该作者未来的所有包。
- 市场签名证明审核过固定字节，不证明代码永远无漏洞，也不替代运行时能力限制。

## 更新、撤销与恢复

- 更新必须由同一创作者指纹签名，SemVer 严格递增；禁止同版本覆盖和换密钥接管。
- 新版独立保存，旧版签名字节不被覆盖。
- 恶意或失陷版本应进入撤销/墓碑数据，重新生成并签名 catalog；客户端优先执行撤销判断。
- 当前维护者信任根仍是单根模型。轮换时必须先发布能信任新根的客户端，再迁移市场签名；
  不得在同一发布中直接替换唯一根导致无法验证更新。

## 报告漏洞

签名、目录、审核工具或市场基础设施漏洞请使用 GitHub Private Vulnerability Reporting：
https://github.com/Starlife-creator/OmniCrawler-market/security/advisories/new

若不可用，请联系 `zqx666666@tutamail.com`。报告请包含影响版本、复现步骤、样本哈希和
建议缓解措施；不要在公开 Issue 中附真实密钥或可利用细节。
