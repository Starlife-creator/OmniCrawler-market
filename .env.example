# 环境变量模板（仅占位符，不含真实密钥）

# 复制为 .env 并填入真实值；.env 已被 .gitignore 拦截，绝不入库。

# 发布者/维护者身份（阶段 1 身份系统启用后使用）
# OMNICRAWL_IDENTITY_USERNAME=your-username
# OMNICRAWL_IDENTITY_PASSWORD=

# 发布前扫描阈值（可选，默认见 tools/scan_plugin.py）
# SCAN_ENTROPY_THRESHOLD=4.5
